{
  "name": "sirkasir-backend",
  "version": "0.7.0",
  "private": true,
  "description": "Sirkasir POS backend - pure Node.js, zero dependencies (no Prisma, no external DB)",
  "scripts": {
    "seed": "node src/seed.js",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
